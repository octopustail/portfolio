import React from 'react';
import '../styles/nav.css'

const enum PathName {
    home = '/',
    skills = '/skills',
    projects = '/projects',
    contact = '/contact',
}

export const About = () => {
    return (
        <div>about</div>
    )
}